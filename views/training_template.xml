<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record model="ir.ui.view" id="train_form_view">
            <field name="name">train.form</field>
            <field name="model">training</field>
            <field name="arch" type="xml">
                <form string="Training Form">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="description"/>
                            <field name="partner_id"/>
                            <field name="user_ids"/>
                            <button name="set_description" string="Set Description" type="object"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <record model="ir.actions.act_window" id="train_list_action">
            <field name="name">Training</field>
            <field name="res_model">training</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">Create the first training
                </p>
            </field>
        </record>

        <!-- top level menu: no parent -->
        <menuitem id="main_training_menu" name="Training"/>
        <menuitem id="train_menu" name="Training" parent="main_training_menu" action="train_list_action"/>

    </data>
</odoo>